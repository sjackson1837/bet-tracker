# Copy this file to ".env" (same folder) and fill in your free key from
# https://the-odds-api.com. Nothing else needs an API key in the local, manual
# setup -- prediction generation happens in the Claude conversation itself.
ODDS_API_KEY=
